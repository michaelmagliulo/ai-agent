services:
  ai-agent:
    build: .
    container_name: ai-agent

    env_file:
      - .env

    ports:
      - "8000:8000"

    restart: unless-stopped

    command: >
      uvicorn src.server:app
      --host 0.0.0.0
      --port 8000